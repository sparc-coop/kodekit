<div class="section">
    <label>
        X Length (px)
        <InputNumber @bind-Value="Shadow.X" />
    </label>
    <label>
        Y Length (px)
        <InputNumber @bind-Value="Shadow.Y" />
    </label>
    <label>
        Blur (px)
        <InputNumber @bind-Value="Shadow.Blur" />
    </label>
    <label>
        Spread(px)
        <InputNumber @bind-Value="Shadow.Spread" />
    </label>
    <label class="color-picker">
        Shadow Color
        <span>
            <input type="color" @bind=Shadow.Color @bind:event="oninput">
            @Shadow.Color
        </span>
    </label>
    <label>
        Shadow Opacity
        <InputNumber @bind-Value="Shadow.Opacity" />
    </label>
</div>

@code {
    [Parameter] public Shadow Shadow { get; set; }
    private Shadow _value;
    [Parameter]
    public Shadow Value
    {
        get => _value;
        set
        {
            if (_value == value) return;
            _value = value;
            ShadowChanged.InvokeAsync(value);
        }
    }
    [Parameter] public EventCallback<Shadow> ShadowChanged { get; set; }

}
